# Discord Bot Configuration
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: All environment variables now require the DRT_ prefix
# This change was made to support the new Cobra/Viper CLI architecture

# Discord Bot Token (Required)
# Get this from https://discord.com/developers/applications
# 1. Create a new application
# 2. Go to the "Bot" section
# 3. Copy the token from the "Token" section
DRT_DISCORD_TOKEN=your_discord_bot_token_here

# Log Level (Optional)
# Available levels: DEBUG, INFO, WARN, ERROR
# Default: INFO
DRT_LOG_LEVEL=INFO

# TTS Configuration (Optional)
# Google Cloud Text-to-Speech credentials path
# Get this from Google Cloud Console by creating a service account
DRT_GOOGLE_CLOUD_CREDENTIALS_PATH=path/to/your/credentials.json

# Default TTS voice (Optional)
# Default: en-US-Standard-A
# Examples: en-US-Standard-A, en-US-Neural2-A, en-GB-Standard-B
DRT_TTS_DEFAULT_VOICE=en-US-Standard-A

# Default speech speed (Optional)
# Range: 0.25 to 4.0, Default: 1.0
DRT_TTS_DEFAULT_SPEED=1.0

# Default speech volume (Optional)
# Range: 0.0 to 2.0, Default: 1.0
DRT_TTS_DEFAULT_VOLUME=1.0

# Maximum queue size per guild (Optional)
# Range: 1 to 100, Default: 10
DRT_TTS_MAX_QUEUE_SIZE=10

# Maximum message length for TTS (Optional)
# Range: 1 to 2000, Default: 500
DRT_TTS_MAX_MESSAGE_LENGTH=500

# Migration from old format:
# If you have existing environment variables without the DRT_ prefix,
# you can quickly migrate them using:
#
# sed -i 's/^DISCORD_TOKEN=/DRT_DISCORD_TOKEN=/' .env
# sed -i 's/^LOG_LEVEL=/DRT_LOG_LEVEL=/' .env
# sed -i 's/^GOOGLE_CLOUD_CREDENTIALS_PATH=/DRT_GOOGLE_CLOUD_CREDENTIALS_PATH=/' .env
# sed -i 's/^TTS_/DRT_TTS_/' .env